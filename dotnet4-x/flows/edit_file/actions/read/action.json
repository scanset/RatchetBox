{
  "$comment": "Read the target file verbatim to ground the edit on its exact current contents. read_file takes -Proj/-Path as command args (filled from the body).",
  "id": "edit_file.read",
  "kind": "action",
  "tool": "read_file",
  "inputs": [
    { "from": "$workspace", "path": ".", "as": "proj" },
    { "from": "path", "path": ".", "as": "path" }
  ],
  "body": { "proj": "{{ proj }}", "path": "{{ path }}" },
  "on_success": "edit_file.generate",
  "on_failure": "edit_file.fail"
}
