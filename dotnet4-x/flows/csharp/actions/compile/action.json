{
  "$comment": "The oracle: csc_check reads the C# on stdin (the tool declares stdin:code) and exits 0 iff it compiles. Tool success -> done; failure -> the one repair attempt.",
  "id": "csharp.compile",
  "kind": "action",
  "tool": "csc_check",
  "inputs": [ { "from": "csharp.generate", "path": ".", "as": "code" } ],
  "body": { "code": "{{ code }}" },
  "on_success": "csharp.done",
  "on_failure": "csharp.fix"
}
