Your previous WinForms program failed to compile. Return a CORRECTED complete program (all `using`
directives, a `[STAThread]` Main, C# 5 subset), fixing exactly what the compiler reported.

## Task
{{ task }}

## Compiler errors
{{ errors }}

## Previous attempt
{{ prev }}

Output ONLY the corrected C# source - no prose, no markdown fences.
