Write a COMPLETE WinForms C# program for the task below. It must compile as a winexe under the in-box
csc (C# 5): include all `using` directives (System, System.Windows.Forms, System.Drawing, ...), a
`[STAThread] static void Main()` that runs the form via Application.Run, and stay in the C# 5 subset.

## Task
{{ task }}

## Reference material (from the knowledge base)
{{ refs }}

Output ONLY the C# source - no prose, no markdown fences.
