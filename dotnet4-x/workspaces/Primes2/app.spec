name: Primes
intent: print the first N prime numbers
behavior:
  - read N from the first command-line arg, default 10
  - print the first N primes, one per line
constraints: C# 5, single file, no external packages
