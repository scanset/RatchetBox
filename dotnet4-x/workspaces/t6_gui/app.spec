name: Greeter
intent: a WinForms window that greets the user
behavior:
  - a window containing a textbox, a button labeled Greet, and a label
  - clicking the Greet button sets the label text to "Hello, " followed by whatever is in the textbox
constraints: C# 5, WinForms, single file, no external packages
