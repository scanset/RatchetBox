{
  "$comment": "TEST rung: author the test(s) from the specs' behavior, matching the stub signatures EXACTLY. Includes a property/fuzz target. On a red-gate failure the flow loops back here; `feedback` carries tdd_red's verdict (empty on the first pass).",
  "id": "tdd.test", "kind": "generate", "prompt": "./prompt.md",
  "inputs": [
    { "from": "tdd.read", "path": ".", "as": "specs" },
    { "from": "tdd.stub", "path": ".", "as": "stub", "max_chars": 4000 },
    { "from": "tdd.red", "path": ".", "as": "feedback", "max_chars": 1500 }
  ],
  "on_success": "tdd.red", "on_failure": "tdd.fail"
}
