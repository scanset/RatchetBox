{
  "$comment": "The oracle: go_build reads the Go source on stdin (the tool declares stdin:code) and exits 0 iff it type-checks. Tool success -> done; failure -> the one repair attempt.",
  "id": "go.build",
  "kind": "action",
  "tool": "go_build",
  "inputs": [ { "from": "go.generate", "path": ".", "as": "code" } ],
  "body": { "code": "{{ code }}" },
  "on_success": "go.done",
  "on_failure": "go.fix"
}
