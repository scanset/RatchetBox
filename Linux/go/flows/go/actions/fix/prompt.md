Your previous Go failed to build. Return a CORRECTED complete Go source file. Fix exactly what the
compiler reported.

- Keep `package solution` (a library package - no `func main`).
- Include every `import` you use; remove any unused import or variable (Go rejects those).

## Task
{{ task }}

## Build errors
{{ errors }}

## Previous attempt
{{ prev }}

Output ONLY the corrected Go source - no prose, no markdown fences.
