Write a COMPLETE Go source file for the task below. It must type-check under `go build`:

- Begin with `package solution` (a library package - do NOT write `func main`).
- Include every `import` you use, and use every import and variable you declare (Go rejects unused
  imports and unused locals).
- Implement the requested function(s) and any types they need.
- Target the standard library only unless the task says otherwise.

## Task
{{ task }}

## Reference material (from the knowledge base)
{{ refs }}

Output ONLY the Go source - no prose, no markdown fences.
