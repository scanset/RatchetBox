{
  "$comment": "The exact existing API of the WHOLE module - so the regenerated units call the UNCHANGED units (Job, Queue, ...) by their real names. The changed units themselves are replaced per their specs, so where the spec and this surface disagree, the spec wins. Grounding, not a gate: on_failure still proceeds.",
  "id": "evolve.api",
  "kind": "action",
  "tool": "module_api",
  "inputs": [ { "from": "$workspace", "path": ".", "as": "proj" } ],
  "body": { "proj": "{{ proj }}" },
  "on_success": "evolve.generate",
  "on_failure": "evolve.generate"
}
