{
  "$comment": "Oracle: route_score gates the KB selection (grounding + coverage) against the catalog + per-lib manifests (task as {query}, planner JSON on stdin). PASS -> draft the spec(s). FAIL -> loop back to spec.plan, which re-plans using this verdict as `feedback` (bounded by max_steps; exhausting it -> spec.fail via the budget).",
  "id": "spec.gate",
  "kind": "action",
  "tool": "route_score",
  "inputs": [
    { "from": "$input", "path": ".", "as": "task" },
    { "from": "spec.plan", "path": ".", "as": "plan" }
  ],
  "body": { "query": "{{ task }}", "plan": "{{ plan }}" },
  "on_success": "spec.generate",
  "on_failure": "spec.plan"
}
