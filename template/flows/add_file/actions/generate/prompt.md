Write a NEW <LANGUAGE> file `{{ path }}` for the project, fulfilling the request below. It must compile
when built as part of the WHOLE project (build_project is the oracle). Match the project's existing
conventions, and include/import everything the file needs.

CHANGE_ME: name your language and the constraints the oracle enforces - standard/version, any forbidden
syntax, and the module/namespace convention - so the model stays inside them.

## Request
{{ request }}

## Relevant references
{{ refs }}

Output ONLY the complete source of {{ path }} - no prose, no markdown fences.
