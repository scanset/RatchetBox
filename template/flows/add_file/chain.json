{
  "$comment": "add_file: create a NEW file in the active workspace (the project). Generate it, stage+build the WHOLE project (the oracle), repair up to twice, then register it in the project's on-disk memory. proj = $workspace; path + request from $input.",
  "id": "add_file",
  "version": "1.1.0",
  "summary": "Add a new file/class/driver/feature to the active workspace project; rebuild the whole project to verify, repair up to twice, and record it.",
  "entry": "add_file.generate",
  "inputs": ["path", "request"],
  "budgets": { "max_steps": 14, "max_total_tokens": 22000, "max_wallclock_seconds": 1200 },
  "nodes": ["add_file.generate", "add_file.build", "add_file.fix", "add_file.rebuild", "add_file.fix2", "add_file.rebuild2", "add_file.register", "add_file.done", "add_file.fail"]
}
