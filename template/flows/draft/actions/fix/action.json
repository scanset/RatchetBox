{
  "$comment": "The repair: regenerate given the oracle's complaint + the previous attempt. Bounded - one fix, then recheck. To repair twice, copy this to fix2 (reading from draft.fix + draft.recheck) and a recheck2, then point recheck.on_failure at draft.fix2.",
  "id": "draft.fix",
  "kind": "generate",
  "prompt": "./prompt.md",
  "inputs": [
    { "from": "$input", "path": ".", "as": "task" },
    { "from": "draft.generate", "path": ".", "as": "prev", "max_chars": 4000 },
    { "from": "draft.check", "path": ".", "as": "errors", "max_chars": 2000 }
  ],
  "on_success": "draft.recheck",
  "on_failure": "draft.fail"
}
