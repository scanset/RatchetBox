{
  "$comment": "Re-run the oracle on the repaired artifact. Success -> done; failure -> fail (one repair exhausted).",
  "id": "draft.recheck",
  "kind": "action",
  "tool": "example_check",
  "inputs": [ { "from": "draft.fix", "path": ".", "as": "code" } ],
  "body": { "code": "{{ code }}" },
  "on_success": "draft.done",
  "on_failure": "draft.fail"
}
