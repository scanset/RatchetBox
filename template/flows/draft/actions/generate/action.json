{
  "$comment": "Generate the artifact for the task ($input), grounded in the top-k kb entries (search binding). A node sees ONLY its declared inputs. Tool/seat success -> check; an empty generation -> fail.",
  "id": "draft.generate",
  "kind": "generate",
  "prompt": "./prompt.md",
  "inputs": [
    { "from": "$input", "path": ".", "as": "task" },
    { "search": "kb", "query": "{{ task }}", "k": 2, "as": "refs", "max_chars": 2000 }
  ],
  "on_success": "draft.check",
  "on_failure": "draft.fail"
}
