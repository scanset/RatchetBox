{
  "$comment": "The oracle: example_check reads the artifact on stdin (the tool declares stdin:code) and exits 0 iff it passes, printing its complaint on failure. Replace example_check with your real validator (a compiler, a parser, the table oracle). Success -> done; failure -> the one repair.",
  "id": "draft.check",
  "kind": "action",
  "tool": "example_check",
  "inputs": [ { "from": "draft.generate", "path": ".", "as": "code" } ],
  "body": { "code": "{{ code }}" },
  "on_success": "draft.done",
  "on_failure": "draft.fix"
}
