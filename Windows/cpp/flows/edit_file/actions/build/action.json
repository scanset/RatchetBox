{
  "$comment": "The oracle: write the candidate into the project's src tree and build the WHOLE project. -Proj/-Path are command args; the code is on stdin (stage_and_build declares stdin:code).",
  "id": "edit_file.build",
  "kind": "action",
  "tool": "stage_and_build",
  "inputs": [
    { "from": "$workspace", "path": ".", "as": "proj" },
    { "from": "path", "path": ".", "as": "path" },
    { "from": "edit_file.generate", "path": ".", "as": "code" }
  ],
  "body": { "proj": "{{ proj }}", "path": "{{ path }}", "code": "{{ code }}" },
  "on_success": "edit_file.log",
  "on_failure": "edit_file.fix"
}
