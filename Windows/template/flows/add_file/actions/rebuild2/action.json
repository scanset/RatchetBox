{
  "$comment": "Build the second repair. Success registers the file; failure aborts (two repairs exhausted).",
  "id": "add_file.rebuild2",
  "kind": "action",
  "tool": "stage_and_build",
  "inputs": [
    { "from": "$workspace", "path": ".", "as": "proj" },
    { "from": "path", "path": ".", "as": "path" },
    { "from": "add_file.fix2", "path": ".", "as": "code" }
  ],
  "body": { "proj": "{{ proj }}", "path": "{{ path }}", "code": "{{ code }}" },
  "on_success": "add_file.register",
  "on_failure": "add_file.fail"
}
