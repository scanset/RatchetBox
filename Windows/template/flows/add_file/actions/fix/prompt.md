Your new file `{{ path }}` failed to build the project. Return the CORRECTED complete file, fixing exactly
what the build reported while still fulfilling the request.

CHANGE_ME: add language-specific error hints here - map your oracle's common errors to their fixes.

## Request
{{ request }}

## Build errors
{{ errors }}

## Your previous version of {{ path }}
{{ prev }}

Output ONLY the complete corrected source of {{ path }} - no prose, no markdown fences.
