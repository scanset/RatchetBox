Write a self-contained <LANGUAGE> snippet for the request below. Output ONLY the source - no prose, no
markdown fences.

CHANGE_ME: name your language and the constraints the `check` oracle enforces (standard/version, any
forbidden syntax).

## Request
{{ task }}

## Relevant references
{{ refs }}
