Write a COMPLETE C# compilation unit for the task below. It must compile under the in-box csc
(C# 5): include every `using` directive you rely on (e.g. System, System.Text, System.Collections.Generic),
and stay in the C# 5 subset (no string interpolation, `?.`, expression-bodied members, tuples).

## Task
{{ task }}

## Reference material (from the knowledge base)
{{ refs }}

Output ONLY the C# source - no prose, no markdown fences.
