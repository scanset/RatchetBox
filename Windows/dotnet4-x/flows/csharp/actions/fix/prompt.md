Your previous C# failed to compile. Return a CORRECTED complete C# compilation unit (all `using`
directives included, C# 5 subset). Fix exactly what the compiler reported.

## Task
{{ task }}

## Compiler errors
{{ errors }}

## Previous attempt
{{ prev }}

Output ONLY the corrected C# source - no prose, no markdown fences.
