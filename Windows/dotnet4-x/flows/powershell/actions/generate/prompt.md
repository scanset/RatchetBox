Write a PowerShell 5.1 script for the task below. It must parse cleanly (syntax only - it will be
parse-checked, not executed).

## Task
{{ task }}

## Reference material (from the knowledge base)
{{ refs }}

Output ONLY the PowerShell script - no prose, no markdown fences.
