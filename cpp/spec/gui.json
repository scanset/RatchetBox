{
  "$comment": "GUI vocabulary for the cpp ratchet (win32-gui domain). A SECOND vocabulary on the same generic engine - proves the spec flexes per domain. Windows/panels contain widgets; widgets raise events to handlers; handlers carry behavior. Validation + grounding only; the spec->Win32 mapping lives in the implement step.",
  "profile": "win32-gui",
  "version": "0.1.0",
  "kinds": {
    "window":  { "behavior": true },
    "panel":   { "behavior": false },
    "button":  { "behavior": false },
    "label":   { "behavior": false },
    "textbox": { "behavior": false },
    "handler": { "behavior": true }
  },
  "edges": {
    "contains":  { "from": ["window", "panel"], "to": ["panel", "button", "label", "textbox"] },
    "on_click":  { "from": ["button"], "to": ["handler"] },
    "on_change": { "from": ["textbox"], "to": ["handler"] }
  },
  "types": ["string", "int", "bool"],
  "methodModifiers": []
}
