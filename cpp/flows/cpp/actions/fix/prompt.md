Your C++ did not compile with MSVC `cl`. Return the corrected COMPLETE source, fixing exactly what the
compiler reported while still solving the task. Stay in C++17; include every header you use.

## Task
{{ task }}

## Compiler errors (cl)
{{ errors }}

## What those errors mean (MSVC docs)
{{ errhelp }}

## Your previous version
{{ prev }}

Output ONLY the corrected C++ source - no prose, no commentary, no markdown code fences (no ```). Use
plain ASCII only: straight quotes (" '), hyphen-minus (-), no smart quotes, em dashes, or bullets.
