For the C++ task below, decide which reference sources you need and write ONE focused search query for
each field. Leave a field as an empty string "" if that source is not relevant - do not force it.

- cppref_q: standard-library headers, APIs, and data structures to look up (e.g. "std::filesystem
  directory_iterator file_size", "std::unordered_map insert erase"). Almost always needed.
- patterns_q: a design pattern to apply, if the task's structure calls for one (e.g. "factory method",
  "observer", "strategy", "RAII wrapper"). Empty for small or procedural tasks.
- win32_q: a Windows API to look up, ONLY if the task is Windows-specific (e.g. "CreateWindowEx message
  loop WndProc", "ReadFile overlapped"). Empty for portable / standard-library-only tasks.
- cppdocs_q: MSVC compiler, build, or C-runtime specifics, if relevant (e.g. "cl /std:c++17 options",
  "_CRT_SECURE_NO_WARNINGS", "fopen_s"). Empty unless the task involves toolchain specifics.

## Task
{{ task }}
