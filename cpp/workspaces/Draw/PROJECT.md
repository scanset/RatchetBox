# Draw

## Purpose
One or two lines: what this is.

## Build
MSVC `cl /std:c++17` over src\**\*.cpp -> dist\Draw.exe (tools/build_project, subsystem console).

## Status / TODO
- [x] scaffold
- [ ] (next feature)

## Changelog
- 2026-06-23  scaffolded (console, c++17)
- 2026-06-23  edited src/core/Circle.h (Implement every method body marked with a // TODO comment: replace the `return {};` stub with real C++ that does what the TODO text says. Keep all signatures, the override keyword, inheritance, fields, and includes exactly as they are. Use valid C++: string literals in double quotes, 3.14159265358979 for pi.)
- 2026-06-23  edited src/core/Rectangle.h (Implement every method body marked with a // TODO comment: replace the `return {};` stub with real C++ that does what the TODO says. Keep all signatures, override, inheritance, fields, includes. Valid C++: string literals in double quotes.)
- 2026-06-23  edited src/core/Triangle.h (Implement every method body marked with a // TODO comment: replace the `return {};` stub with real C++ that does what the TODO says. Keep all signatures, override, inheritance, fields, includes. Valid C++: string literals in double quotes.)
- 2026-06-23  edited src/core/Circle.h (Implement every method body marked with a // TODO comment: replace the `return {};` stub with real C++ doing what the TODO says. Keep all signatures, override, the constructor, inheritance, fields, includes. Valid C++: string literals in double quotes, 3.14159265358979 for pi.)
- 2026-06-23  edited src/core/Rectangle.h (Implement every method body marked with a // TODO comment: replace the `return {};` stub with real C++ doing what the TODO says. Keep all signatures, override, the constructor, inheritance, fields, includes. Valid C++: string literals in double quotes, 3.14159265358979 for pi.)
- 2026-06-23  edited src/core/Triangle.h (Implement every method body marked with a // TODO comment: replace the `return {};` stub with real C++ doing what the TODO says. Keep all signatures, override, the constructor, inheritance, fields, includes. Valid C++: string literals in double quotes, 3.14159265358979 for pi.)
