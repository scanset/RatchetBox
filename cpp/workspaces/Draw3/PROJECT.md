# Draw3

## Purpose
One or two lines: what this is.

## Build
MSVC `cl /std:c++17` over src\**\*.cpp -> dist\Draw3.exe (tools/build_project, subsystem console).

## Status / TODO
- [x] scaffold
- [ ] (next feature)

## Changelog
- 2026-06-23  scaffolded (console, c++17)
- 2026-06-23  edited src/core/Circle.h (implement every method body marked with a // TODO comment using the pseudocode in that comment; replace the return {}; stub with real C++. Keep every signature, the override keyword, inheritance, fields, the constructor, and includes exactly as they are.)
- 2026-06-23  edited src/core/Rectangle.h (implement every method body marked with a // TODO comment using the pseudocode in that comment; replace the return {}; stub with real C++. Keep every signature, the override keyword, inheritance, fields, the constructor, and includes exactly as they are.)
- 2026-06-23  edited src/core/Triangle.h (implement every method body marked with a // TODO comment using the pseudocode in that comment; replace the return {}; stub with real C++. Keep every signature, the override keyword, inheritance, fields, the constructor, and includes exactly as they are.)
